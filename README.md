# Homebrew tap for [cold-frame](https://github.com/coldzero94/cold-frame)

Local-first, ownable memory for LLM agents — one offline SQLite file. Plugs into Claude Code.

```bash
brew install coldzero94/coldframe/cold-frame
```

The formula installs a self-contained binary (CLI + MCP server in one file — no Python needed).
